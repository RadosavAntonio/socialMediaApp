import React from 'react'
import {Text} from 'react-native-svg'
import {Screen} from '../../../navigation/navigation'

export const Profile = (): JSX.Element => {
  return <Text>{Screen.PROFILE}</Text>
}
